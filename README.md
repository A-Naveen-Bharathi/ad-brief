# ad-brief
A single-file HTML tool for generating structured ad briefs across Google, Meta, LinkedIn, Reddit, and Microsoft/Bing. Select formats, platforms, and set quantities, then export a formatted Excel file with copy fields, character limits, live counters, and design specs. No dependencies, no install.
